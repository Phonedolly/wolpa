//! ## wolpa-cli — Debug CLI tool
//!
//! A minimal command-line binary for testing `wolpa-core` without the macOS app.
//! Spawns `nvim --embed`, attaches as a UI, and dumps redraw events for debugging.

fn main() {
    println!("wolpa-cli: debug tool for wolpa-core");
}
