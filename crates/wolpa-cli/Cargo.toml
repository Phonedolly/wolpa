[package]
name = "wolpa-cli"
version = "0.1.0"
edition = "2021"
description = "Debug CLI for testing RPC and grid without the macOS app"

[dependencies]
wolpa-core = { path = "../wolpa-core" }
tokio = { version = "1", features = ["process", "io-util", "rt-multi-thread", "macros"] }
